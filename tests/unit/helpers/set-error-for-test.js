import { setErrorFor } from 'abc/helpers/set-error-for';
import { module, test } from 'qunit';

module('Unit | Helper | set error for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = setErrorFor([42]);
  assert.ok(result);
});
