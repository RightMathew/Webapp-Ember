import { deptIndex } from 'abc/helpers/dept-index';
import { module, test } from 'qunit';

module('Unit | Helper | dept index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = deptIndex([42]);
  assert.ok(result);
});
